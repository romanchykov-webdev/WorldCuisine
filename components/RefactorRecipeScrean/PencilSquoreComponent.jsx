import { StyleSheet, View } from 'react-native'
import { PencilSquareIcon } from 'react-native-heroicons/outline'

function PencilSquoreComponent() {
  return (
    <View>
      <PencilSquareIcon size={hp(4)} strokeWidth={2.5} color="gray" />
    </View>
  )
}

const styles = StyleSheet.create({})

export default PencilSquoreComponent
